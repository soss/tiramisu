$("#<%= dom_id(@project) %> .promote a").addClass('disabled');
$("#<%= dom_id(@project) %> .score").text("<%= @points %>");
$("#<%= dom_id(@project) %> .suffix").text("<%= 'vote'.pluralize(@points) %>");